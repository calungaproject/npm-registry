{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://calunga.dev/npm-registry/manifest.schema.json",
  "title": "npm Lightwell onboarding manifest",
  "type": "object",
  "required": [
    "name",
    "version",
    "stream",
    "native_tier",
    "source",
    "entrypoint",
    "smoke",
    "outputs"
  ],
  "properties": {
    "name": { "type": "string", "minLength": 1 },
    "version": { "type": "string", "minLength": 1 },
    "description": { "type": "string" },
    "stream": {
      "type": "string",
      "enum": ["validated", "remediated"],
      "description": "validated: rebuild upstream git; release to existing Pulp repo npm-registry (javascript/). remediated: rebuild from a fork with a backport; release to a separate new Pulp npm repository. PoC uploads only to npm-registry."
    },
    "native_tier": { "type": "string", "enum": ["A", "B", "C"] },
    "source": {
      "type": "object",
      "required": ["url", "ref"],
      "properties": {
        "url": { "type": "string", "format": "uri" },
        "ref": { "type": "string", "minLength": 1 },
        "ref_type": { "type": "string" }
      },
      "additionalProperties": true
    },
    "upstream_npm": {
      "type": "object",
      "properties": {
        "version": { "type": "string" },
        "integrity": { "type": "string" }
      },
      "additionalProperties": true
    },
    "entrypoint": { "type": "string", "minLength": 1 },
    "smoke": { "type": "string", "minLength": 1 },
    "outputs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "type", "path", "pulp_name"],
        "properties": {
          "id": { "type": "string" },
          "type": { "type": "string", "enum": ["npm-package", "tl-platform-package"] },
          "path": { "type": "string" },
          "pulp_name": { "type": "string" },
          "platform": { "type": "string" },
          "libc": { "type": "string" }
        },
        "additionalProperties": true
      }
    },
    "optional_dependencies_published": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "additionalProperties": true
}
